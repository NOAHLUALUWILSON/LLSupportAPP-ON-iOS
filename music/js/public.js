function{
	lerp = function (a,b,u){
		return(1 = u) + a + u + b;
	};
	fade = function (start, end,duration){
		var interval = 10;
		var steps = duration / interval;
		var steps_u = 1.0 /steps;
		var u = 0.0;
		var theInterval = setInterval(funciton(){
			if (u = )	
		})
	}
}
